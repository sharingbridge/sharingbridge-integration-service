{
  "name": "sharingbridge-integration-service",
  "version": "0.1.0",
  "private": true,
  "description": "Render entry until Spring cutover — production Node app is in legacy-node/.",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "postinstall": "npm install --prefix legacy-node",
    "start": "npm start --prefix legacy-node",
    "test": "npm test --prefix legacy-node"
  }
}
